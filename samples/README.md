# Reserved for kernel / conformance samples

Public installable apps live under `agents/` and are listed in `index.json`.

Integration-test fixtures for engine-router conformance live in
`korun/crates/ko-kernel/tests/fixtures/`.